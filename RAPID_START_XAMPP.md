# 🐍 راهنمای کامل اجرای بازی مار در XAMPP

## ✅ فایل‌های آماده شده

در پوشه `dist` سه فایل برای اجرای بازی در XAMPP ایجاد شده است:

1. **index.php** - نسخه کامل با قابلیت‌های پیشرفته
2. **play.php** - نسخه ساده و سریع
3. **README-XAMPP.md** - راهنمای فارسی

---

## 🚀 روش سریع (۳ دقیقه‌ای)

### مرحله ۱: کپی پوشه
کل پوشه `dist` را به مسیر زیر منتقل کنید:
```
C:\xampp\htdocs\snake-game\
```

### مرحله ۲: اجرای XAMPP
1. XAMPP Control Panel را باز کنید
2. دکمه **Start** کنار **Apache** را بزنید
3. صبر کنید تا سبز شود

### مرحله ۳: اجرای بازی
مرورگر را باز کرده و به آدرس زیر بروید:
```
http://localhost/snake-game/play.php
```

**تمام! بازی اجرا شد! 🎮**

---

## 📁 ساختار پوشه‌ها

بعد از کپی، ساختار باید به این شکل باشد:

```
htdocs/
└── snake-game/
    ├── play.php           ← ساده‌ترین روش اجرا
    ├── index.php          ← نسخه پیشرفته
    ├── index.html         ← فایل HTML اصلی
    └── assets/
        ├── index-EWW6Bgju.css
        └── index-UaGRYqR2.js
```

---

## 🔍 تفاوت فایل‌های PHP

### play.php (پیشنهادی برای تست سریع)
- ✅ بسیار ساده
- ✅ فقط فایل HTML را لود می‌کند
- ✅ بدون پردازش اضافی
- مناسب برای: تست سریع و محیط‌های ساده

### index.php (نسخه پیشرفته)
- ✅ بررسی خودکار فایل‌ها
- ✅ لود داینامیک CSS و JS
- ✅ هدربهرهای بهینه‌سازی
- ✅ پشتیبانی از کش
- مناسب برای: محیط production و سرور واقعی

---

## 🌐 آدرس‌های مختلف اجرا

بسته به نام پوشه و پورت، می‌توانید از آدرس‌های زیر استفاده کنید:

| حالت | آدرس |
|------|------|
| پیش‌فرض | `http://localhost/snake-game/play.php` |
| پورت 8080 | `http://localhost:8080/snake-game/play.php` |
| بدون PHP | `http://localhost/snake-game/` |
| نسخه پیشرفته | `http://localhost/snake-game/index.php` |

---

## ⚠️ رفع مشکلات رایج

### ❌ خطای 404: Not Found
**علت:** مسیر فایل‌ها اشتباه است

**راه‌حل:**
1. مطمئن شوید پوشه `snake-game` در `htdocs` قرار دارد
2. بررسی کنید که فایل `play.php` وجود داشته باشد
3. آدرس را دقیقاً به صورت زیر وارد کنید:
   ```
   http://localhost/snake-game/play.php
   ```

### ❌ خطای 403: Forbidden
**علت:** دسترسی به پوشه ندارید

**راه‌حل:**
1. روی پوشه `snake-game` کلیک راست کنید
2. Properties → Security را باز کنید
3. به کاربر فعلی دسترسی Full Control بدهید

### ❌ بازی لود می‌شود اما کار نمی‌کند
**علت:** فایل‌های JS/CSS لود نمی‌شوند

**راه‌حل:**
1. کلید F12 را بزنید
2. به تب Console بروید
3. خطاهای قرمز را بررسی کنید
4. اگر خطای CORS دارید، از `play.php` استفاده کنید

### ❌ Apache شروع نمی‌شود
**علت:** پورت 80 اشغال است

**راه‌حل:**
1. XAMPP Control Panel → Config → httpd.conf
2. خط `Listen 80` را به `Listen 8080` تغییر دهید
3. Apache را Restart کنید
4. از آدرس `http://localhost:8080/snake-game/play.php` استفاده کنید

---

## 🎮 کنترل‌های بازی

| ورودی | عملکرد |
|-------|--------|
| ↑ ↓ ← → | حرکت مار |
| Swipe (موبایل) | تغییر جهت |
| Space | شروع/توقف |

---

## 💡 نکات مهم

1. **کش مرورگر**: اگر تغییرات را نمی‌بینید، Ctrl+F5 را بزنید
2. **ذخیره رکورد**: رکوردها در LocalStorage ذخیره می‌شوند
3. **موبایل**: بازی کاملاً ریسپانسیو است
4. **آفلاین**: پس از لود اولیه، بدون اینترنت هم کار می‌کند

---

## 📊 مشخصات فنی

- **Framework**: React + Vite
- **Styling**: TailwindCSS
- **Animation**: Framer Motion
- **Storage**: LocalStorage
- **PHP Version**: 7.4+
- **Browser**: Modern (Chrome, Firefox, Edge)

---

## 🎉 لذت ببرید!

حالا می‌توانید بازی مار را در XAMPP اجرا کرده و از آن لذت ببرید!

اگر سوالی داشتید، فایل `README-XAMPP.md` را مطالعه کنید.

**موفق باشید! 🐍✨**
